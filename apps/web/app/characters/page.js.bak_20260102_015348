import CharactersClient from "./CharactersClient";

export const metadata = { title: "Characters — AI Content Workstation" };

export default function CharactersPage() {
  return <CharactersClient />;
}
